{
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "env": [
        "NODE_ENV",
        "PORT",
        "API_URL",
        "WEB_URL",
        "DATABASE_URL",
        "REDIS_URL",
        "GOOGLE_CLIENT_ID",
        "GOOGLE_CLIENT_SECRET",
        "SESSION_ENCRYPTION_KEY"
      ]
    }
  }
}
